FROM python:3.10-slim

WORKDIR /app
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir vllm uvicorn

EXPOSE 8000

CMD ["python", "-m", "vllm.entrypoints.openai.api_server", "--model", "distilgpt2", "--host", "0.0.0.0", "--port", "8000"]