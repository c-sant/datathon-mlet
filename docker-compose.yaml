version: "3.9"

services:
  pipeline:
    build: .
    container_name: datathon-mlet
    working_dir: /app
    volumes:
      - .:/app
    command: dvc repro

  mlflow:
    build: .
    container_name: datathon-mlflow-ui
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5000:5000"
    command: mlflow ui --host 0.0.0.0 --backend-store-uri sqlite:///mlflow/mlflow.db